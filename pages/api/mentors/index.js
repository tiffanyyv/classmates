// POST: /pages/mentors/index.js

export default function addMentor(req, res) {
  const { account_type, email, firstName, lastName, location, password, uid, username } = req.body;
  // open to add query
}